# ===============================================
# Streamlit Secrets Configuration (for Cloud)
# ===============================================
# This file stores sensitive credentials used by your app.
# It should NEVER be committed to GitHub.
# You configure this content in Streamlit Cloud → App Settings → Secrets.

# Top-level keys used by app.py
# Prefer using your ANON/PUBLIC key here, not the service role.
SUPABASE_URL = "https://fuynmwpwcekkyfkiaunu.supabase.co"
# Replace with your ANON/PUBLIC API key from Supabase → Project Settings → API
SUPABASE_KEY = "sb_publishable_DsNmO3_ZfeA-F2EvYGRQfw_nv05xSRL"

[supabase]
# Your Supabase project URL (found in Project Settings → API)
url = "https://fuynmwpwcekkyfkiaunu.supabase.co"

# Use your PUBLISHABLE or ANON key (safe for public usage)
# ⚠️ DO NOT use the Service Role Key here (it can modify schema and bypass security)
# Replace with your ANON/PUBLIC API key (same as SUPABASE_KEY above)
key = "sb_publishable_DsNmO3_ZfeA-F2EvYGRQfw_nv05xSRL"

# Optional: define table names used by your app
users_table = "users"
coaches_table = "coaches"
schools_table = "schools"
materials_table = "materials"
requests_table = "requests"

# Optional metadata for debugging
project_name = "School Jiu-Jitsu Program"
environment = "production"
